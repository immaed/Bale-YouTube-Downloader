name: "⏰ Cron Trigger"

on:
  schedule:
    - cron: '*/1 * * * *'  # هر 1 دقیقه
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: "Ping cron-check.php"
        run: |
          curl -s "https://khashayar.one/cron-check.php" || echo "Failed"
